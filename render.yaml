services:
  - type: web
    name: expense-tracker
    runtime: python
    buildCommand: ""
    startCommand: python main.py
    envVars:
      - key: FLASK_ENV
        value: production
    autoDeploy: true
